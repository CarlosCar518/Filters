The code is shitty and has very bad performace, I will try to fix that in the future.

## LIMITATIONS
- It does not process images with padding. 4|width must be true.
- It is not interactive, you have to modify the code to change the filters.
- Only processes images with 24 bits per px.
- It does not work with compressed images.

